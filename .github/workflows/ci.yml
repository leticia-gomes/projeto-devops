name: CI com Docker

on:
  push:
    branches: [ "main" ]

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Build Docker
        run: docker build -t projeto-devops .

      - name: Run Docker
        run: docker run projeto-devops
